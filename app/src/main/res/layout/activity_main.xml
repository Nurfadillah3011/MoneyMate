<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/backgroundColor">

    <!-- Header yang akan tetap muncul -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:orientation="horizontal"
        android:background="?attr/headerBackgroundColor"
        android:padding="16dp"
        android:gravity="center_vertical"
        app:layout_behavior="com.google.android.material.appbar.AppBarLayout$ScrollingViewBehavior">

        <ImageView
            android:layout_width="65dp"
            android:layout_height="90dp"
            android:src="@drawable/finflex"
            android:background="?attr/headerBackgroundColor"
            android:contentDescription="Logo FinFlex"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter" />

        <!-- Tambahkan ini untuk button tema di kanan -->
        <Space
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1" />

        <ImageButton
            android:id="@+id/btn_theme"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_theme"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Ubah tema"
            android:layout_marginEnd="8dp"
            app:tint="?attr/iconTintColor" />
    </LinearLayout>

    <!-- Fragment container dengan margin top sesuai tinggi header -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="60dp"
        android:layout_marginBottom="56dp"
    app:defaultNavHost="true"
    app:navGraph="@navigation/nav_graph" />

    <!-- Bottom Navigation View -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="?attr/cardBackgroundColor"
        app:menu="@menu/main_menu"
        app:elevation="8dp"
        app:itemIconTint="?attr/iconTintColor"
        app:itemTextColor="?attr/textPrimaryColor" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>